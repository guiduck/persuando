# Codex Entry Point

Read `AGENTS.md` first. It is the Codex top-level project brief.

When a task matches a mirrored workflow skill, load the matching file under `.codex/skills/`.
The canonical long-form skill instructions live under `.cursor/skills/`.

